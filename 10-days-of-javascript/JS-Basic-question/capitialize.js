/*
JavaScript function to capitalize the first letter of a string.
*/

function capitialize(str) {
    return str.charAt(0).toUpperCase() + str.slice(1)

}
console.log(capitialize("js string exercises"))